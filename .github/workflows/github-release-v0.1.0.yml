name: Publish GitHub v0.1.0

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  frozen:
    name: v0.1.0 release already published
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "AgentBlindspot v0.1.0 is already published from b1014573264feab70cebc198e24e4598a414c65d."
          echo "This workflow is intentionally frozen to avoid recreating the tag or release."
