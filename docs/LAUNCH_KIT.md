# Launch kit

Use only after release gates are green and the repository/package are actually accessible.

## Core story

Coding agents show what they did. AgentBlindspot independently joins tool evidence with repository dependency structure to show potentially affected code with no observed inspection or direct verification.

**Qualification:** possible blind spot does not mean proven bug.

## GitHub release

> AgentBlindspot 0.1.0 is a local-first evidence analyzer for coding-agent workflows. It distinguishes changed, structurally impacted, inspected, directly verified, possible-blind-spot, and unknown files without requiring a second LLM. The release includes a reproducible auth example, standalone HTML/JSON output, bounded Codex ingestion, Claude/OpenCode structured adapters, JS/TS + Python graphs, coverage ingestion, and exact-SHA release evidence.

## Hacker News / Reddit

> I built AgentBlindspot because coding-agent transcripts are chronological while code dependencies are structural. It overlays observed tool activity on a deterministic repository graph and highlights *possible* blind spots—files exposed to a change with no observed inspection or direct coverage. Local-only, no API key, and no second LLM judging the first. The repo includes the exact fixture that reproduces the README example.

## Short social copy

> See what your coding agent never checked: changed -> impacted -> inspected -> verified -> possible blind spot. Local-first and deterministic. Possible blind spot != proven bug.

Do not automate unsolicited promotion, fabricate testimonials, buy stars, or exaggerate severity.
